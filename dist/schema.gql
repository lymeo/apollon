scalar Object
directive @auth(roles: [String]) on OBJECT | FIELD_DEFINITION

type Comment {
    id: String
    content: String
    author: String
}
# If you don't have any Query, comment all the file

type Query {
    getAll: [Comment]
    getById(id: ID!): Comment @auth(roles: ["admin"])
}
# If you don't have any Mutation, comment all the file

type Mutation {
    create(data: IComment!): Comment
    delete(id: ID!): Boolean
    update(id: ID!, data: IUComment): Boolean
}
# If you don't have any Input, comment all the file

input IComment {
    content: String!
    author: String!
}

input IUComment {
    content: String
    author: String
}